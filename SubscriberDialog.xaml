<Window x:Class="CableSubscriberApp.SubscriberDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Subscriber"
        Height="400"
        Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="NameBox" Grid.Row="0" Margin="5" ToolTip="Name"/>
        <TextBox x:Name="NickNameBox" Grid.Row="1" Margin="5" ToolTip="Nick Name"/>
        <TextBox x:Name="RentBox" Grid.Row="2" Margin="5" ToolTip="Rent"/>
        <TextBox x:Name="StatusBox" Grid.Row="3" Margin="5" ToolTip="Status"/>
        <TextBox x:Name="AreaBox" Grid.Row="4" Margin="5" ToolTip="Area"/>
        <TextBox x:Name="CompanyBox" Grid.Row="5" Margin="5" ToolTip="Company"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="OK" Width="80" Margin="5" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
